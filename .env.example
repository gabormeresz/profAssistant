# ============================================================================
# ProfAssistant — Environment Configuration
# ============================================================================
# This is the SINGLE source of truth for all environment variables.
#
# Quick start:
#   cp .env.example .env
#   # Edit .env and fill in your values
#
# Works for BOTH local development and Docker.
# ============================================================================


# ── API Keys ───────────────────────────────────────────────────────────────

# Your OpenAI API key for LLM operations (required)
OPENAI_API_KEY=your_openai_api_key_here

# Tavily web search & extract (via remote MCP server) — optional but
# recommended for richer content generation. Requires a Tavily account and API key.
# Comment out if you don't want to use it, but note that some features will be limited without it.
TAVILY_API_KEY=your_tavily_api_key_here


# ── Authentication (required) ──────────────────────────────────────────────
# Generate both secrets with one command (no dependencies needed):
#   python3 -c "import secrets,base64,os; print('JWT_SECRET=' + secrets.token_hex(32)); print('ENCRYPTION_KEY=' + base64.urlsafe_b64encode(os.urandom(32)).decode())"

JWT_SECRET=your_jwt_secret_here
ENCRYPTION_KEY=your_fernet_key_here

# Token expiry
# JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30    # access token lifetime in minutes
# JWT_REFRESH_TOKEN_EXPIRE_DAYS=7       # refresh token lifetime in days


# ── Admin Seed Account (required on first startup) ─────────────────────────

ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change_me_immediately


# ── LangSmith (optional tracing) ───────────────────────────────────────────
# If you want to enable tracing with LangSmith, fill in these values.

# LANGSMITH_TRACING=true
# LANGSMITH_ENDPOINT=https://api.smith.langchain.com
# LANGSMITH_API_KEY=your_langsmith_api_key_here
# LANGSMITH_PROJECT=your_project_name


# ── Cookie Configuration ───────────────────────────────────────────────────
# Defaults are safe for local development — only override in production.

# COOKIE_SECURE=false                   # set to true when serving over HTTPS
# COOKIE_SAMESITE=lax                   # use "strict" in production
# COOKIE_DOMAIN=                        # set to your domain (e.g. yourdomain.com)


# ── CORS Configuration ────────────────────────────────────────────────────
# Comma-separated origins. Defaults cover local dev ports.
# Override in production with your actual domain.

# CORS_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:3000
# ALLOWED_ORIGINS=https://yourdomain.com  # used by docker-compose.prod.yml


# ── MCP Configuration ──────────────────────────────────────────────────────

# MCP_WIKIPEDIA_ENABLED=true            # Wikipedia MCP server toggle

# ── Debug / Testing ────────────────────────────────────────────────────────

# USE_DUMMY_GRAPH=false                 # use hardcoded data instead of LLM
# LOG_LEVEL=INFO                        # DEBUG, INFO, WARNING, ERROR, CRITICAL

# ── Upload Limits ──────────────────────────────────────────────────────────

# MAX_FILE_SIZE=10485760                # max upload size in bytes (10 MB)


